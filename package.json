{
  "name": "jolt-observability-demo-browser-tests",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test:browser": "playwright test --project=chromium",
    "test:browser:persistent": "scripts/test-browser-persistent.sh",
    "docs:screenshots": "playwright test --project=docs",
    "docs:agent-screenshots": "playwright test --project=agent-docs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
